<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TestTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/sf_pro_display</item>
        <item name="android:statusBarColor">@color/white10</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.TestTask" parent="Base.Theme.TestTask" />
</resources>